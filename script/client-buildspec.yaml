version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
  pre_build:
    commands:
      - echo "Installing..."
      - npm install
  build:
    commands:
      - echo "building project..."
      - npm run build

artifacts:
  files:
    - "**/*"
  base-directory: "build"
